{
  "name": "@quarrel/desktop",
  "version": "1.0.9",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "ios:open": "tauri ios dev --open --host",
    "ios:build": "node ./scripts/ios-build.mjs",
    "ios:build:open": "node ./scripts/ios-build.mjs --open",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-shell": "^2.3.5"
  }
}
